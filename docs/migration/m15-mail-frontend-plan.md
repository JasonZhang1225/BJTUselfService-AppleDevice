# M15 邮箱原生前端

状态：**基本完成（代码与首轮真实账号验收已完成；真实发送、删除等写操作仍待明确确认）**

本轮开发在 `maildev` 分支进行，`main` 未提交或推送本轮改动。

## 当前网页取证（2026-08-28）

本轮通过当前 Chrome 的直接 DevTools MCP 读取已登录的北京交通大学邮件系统，只记录页面结构和请求路径，不记录邮件正文、收件人、Cookie 或会话参数。

- 页面标题为“北京交通大学邮件系统”，`body` 使用 `XT5-layout`，可判断这是 Coremail XT5 的网页端。
- 页面没有检测到 React、Vue、Angular 或全局 jQuery；主要资源是 Coremail 自有打包脚本（`X`、`M`、`316`、`986`）和 `X.technology` 样式文件，存在 webpack runtime。
- 桌面结构为：左侧功能栏 → 文件夹栏 → 邮件列表/标签页 → 阅读区；列表、阅读、回复、转发、移动、标记、删除、搜索和写信入口均存在。
- 观察到的请求路径包括 `/coremail/XT/jsp/mail.jsp`、`readMessage.jsp`、`common.jsp`、`/coremail/s/json` 和 Socket.IO 长连接。它们的请求参数和响应结构不能凭路径猜测，下一阶段要用脱敏的 DevTools 网络记录逐项确认。
- 当前 KMP 实现位于 `shared/feature/mailbox`：登录态从 MIS 会话导出 Cookie；iOS 使用 WKWebView，macOS 使用系统浏览器。现有文档已经记录学校网页在移动端是桌面多栏布局，不能直接作为手机原生体验。

## 2026-08-29 继续开发与网页核对

- 通过当前 Chrome 的直接 DevTools MCP 逐项打开实际文件夹，确认 Coremail FID 为收件箱 `1`、待办 `-5`、草稿 `2`、已发送 `3`、已删除 `4`、垃圾邮件 `5`、病毒邮件 `6`；当前登录态收件箱显示 216 封、已发送显示 36 封，其余被核对的内置文件夹为空。未记录邮件正文、地址、Cookie 或带会话参数的 URL。
- Windows 源码版默认邮箱内容区约 820dp，达不到三栏门槛；此前紧凑布局没有文件夹入口，已在 `MailboxScreen.kt` 增加“当前文件夹 / 切换”菜单，复用上述 7 个 FID。视觉点验已完成收件箱→已发送（36 封）和已发送→待办（空状态）。
- Android SDK/`adb`/x86_64 模拟器已恢复到 `C:\Users\zjg\Android\Sdk`；通过 `BJTU_ANDROID_ABIS=arm64-v8a,x86_64` 构建并安装 debug APK 后，用户已完成登录且应用可自动恢复。视觉回归确认“更多 → 邮箱”按教室查询模式启动 `MAILBOX` 原生 Activity；点击邮件继续进入 `MAILBOX_DETAIL` 原生详情 Activity，任务栈保持主 Activity → 邮箱列表 → 邮件详情，平台转场正常。邮箱主页右上角胶囊显示“刷新”；普通刷新只请求当前列表，会话失效时“重试登录”先恢复 CAS/MIS 再拉取邮箱；随后 Android 与 Windows 又核对了合并后的文件夹/列表标题卡。
- 通过当前 Chrome 的直接 DevTools MCP 读取两封真实样本：一封含标准 HTML 表格，另一封含多张 Word/Coremail 表格和嵌入式 `<style>`。网页源内容本身可正常渲染；KMP 原因是旧逻辑把表格标签全部压成纯文本，并把 `<style>` 内的 CSS 规则带进正文。现已改为 DOM 结构化解析，忽略样式/脚本节点，段落与表格分块输出，表格在紧凑端用可横向滚动的网格显示。
- Android x86_64 模拟器已用这两封真实样本完成详情视觉回归：两封邮件均不再显示 CSS 文本，标准表格和多张结果表均显示为带边框网格；超出窄屏的列保留横向滚动。共享桌面测试、Windows/Android 编译均通过。
- 详情返回层级已收口：紧凑端详情不再在正文上方单独显示“返回邮件列表”，邮箱页左上角返回根据当前状态回到邮件列表或邮箱上级；新增 `MAILBOX_COMPOSE` 写信/回复二级路由。写信先调用 `compose.jsp?ctype=normal`，回复调用 `compose.jsp?ctype=reply&mid=...` 取得服务器草稿；正文以纯文本编辑，发送前弹出确认框，再用 `mbox:compose` 的 `action=deliver` 提交，取消/系统返回会尽力清理临时草稿。Android 已视觉验证写信页、回复预填和发送确认；Windows 已视觉验证写信页。未实际发送邮件。

## M15 目标

在不改变学校邮箱账号体系的前提下，使用 KMP 共享状态与 Compose Multiplatform 写一个原生邮件阅读前端。只读收件箱和邮件详情已完成首轮；写信/回复首版已接入，发送仍需真实账号风险评审。

### 信息架构

- iPhone：文件夹列表 → 邮件列表 → 邮件详情，使用平台原生二级页面；搜索和刷新保持在当前上下文附近。
- 内置文件夹按 Coremail 的实际 FID 访问：收件箱 `1`、待办邮件 `-5`、草稿箱 `2`、已发送 `3`；已删除 `4`、垃圾邮件 `5`、病毒邮件 `6` 归入“其他文件夹”。网页树节点序号不能直接当作 FID。
- iPad/macOS：窗口足够宽时显示文件夹、列表、详情三栏；中等宽度退化为列表—详情两栏；窄窗口不强行压缩列宽。
- 邮件线程、未读状态、附件和错误状态使用明确的共享模型；正文 HTML 在受控容器中显示，外链交给系统浏览器。
- 本地默认只缓存必要的列表摘要，不把完整正文、附件或 Cookie 写入普通日志/调试快照；缓存策略需在实现前单独确认。

### Apple Design 方向

- 以系统字体、动态字号、明确的标题/发件人/日期层级构成邮件行，不依赖颜色单独表达未读。
- 交互立即反馈：按下、刷新、标记已读和切换文件夹都要有即时状态；网络等待显示在当前操作附近。
- 详情页进入和返回保持同一空间路径；加载、错误和空状态使用轻量 material surface，不用无意义的弹跳。
- macOS 补齐键盘导航、悬停反馈和可调整列宽；iPhone 保持单手可达和足够的触控命中区；减少动态效果时改用淡入淡出。

## 实施切片

1. **协议取证与适配器**：只读记录收件箱、分页、读取详情、标记已读和刷新所需请求，建立脱敏 fixture；Cookie 仍由现有内存会话边界提供。
2. **邮件前端**：已新增邮箱远端适配、列表/详情状态和 Compose UI；当前覆盖收件箱、待办邮件、草稿箱、已发送、已删除、垃圾邮件和病毒邮件的文件夹切换，刷新、分页加载、已读状态、正文和附件元数据。正文解析已支持段落、换行和 HTML 表格，忽略邮件内嵌样式/脚本文本，表格以可横向滚动网格显示；写信/回复首版已支持服务器草稿初始化、收件人/抄送/主题/正文编辑、发送前确认和临时草稿取消；本地正文缓存和附件上传仍未实现。
3. **响应式平台布局**：已完成 iPhone/紧凑窗口文件夹选择、邮箱列表→邮件详情的平台原生页面，以及 iPad/macOS 宽窗口文件夹—列表—阅读三栏；紧凑端邮箱主页、邮件详情和写信/回复统一使用平台原生页面层级，与两个教室查询入口一致；根 Activity 在详情转场期间保持列表，避免列表→内嵌详情→原生详情的重复视觉跳转；紧凑端文件夹名称、邮件总数和文件夹切换合并为当前文件夹 banner，邮箱右上角胶囊显示“刷新”，避免操作入口在标题区重复或出现破碎图标；普通刷新与会话失效后的“重试登录”已分工，后者会先恢复 CAS/MIS 会话再拉取邮箱；详情唯一返回固定在左上角，紧凑端不再在正文上方重复显示“返回邮件列表”；写信/回复使用同一 `MAILBOX_COMPOSE` 原生编辑页；动态字体、深浅色和离线/失败状态保留在共享布局中，真实登录后的 iOS 邮箱页面仍待验收。
4. **写操作单独评审**：写信/回复首版已实现服务器草稿初始化、编辑和发送前确认，但尚未使用真实账号发送；转发、删除、移动、标记、附件上传/下载逐项确认请求、撤销/确认文案与真实账号风险后再实现。

## 验收门槛

- `shared:desktopTest` 和 `shared:iosSimulatorArm64Test` 覆盖分页、日期、未读、HTML 正文安全边界及失败恢复。
- Mac 窗口验证至少覆盖窄窗口、宽窗口和调整列宽；iOS 模拟器验证列表—详情导航、返回、旋转/尺寸变化和动态字体。
- 真实账号只读验收前不执行发送、删除、移动或上传；截图与日志不得包含真实邮件正文、地址、电话、Cookie 或 URL 会话参数。共享层邮箱/导航/CAS 恢复回归测试、Android x86_64 debug 构建及 Windows/Android 邮箱主页与详情视觉回归已通过；两封真实 HTML 表格邮件的 Android 详情回归、写信页、回复预填和发送确认回归已通过，未实际发送邮件。Windows 写信页视觉回归已通过；最终一次 Xcode Simulator 构建被用户中断，因此 iOS 新 UI 的真实登录视觉验收仍待补做。
- 若 Coremail 私有协议不稳定，保留现有 WebView/系统浏览器作为回退，不为了“原生化”而绕过学校权限或 TLS 安全。
