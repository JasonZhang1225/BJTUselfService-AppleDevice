# v1.7.0 功能与迁移矩阵

> 基线：`v1.7.0@419313d`  
> 当前状态：官方 APK 已完成登录，一级页面和核心安全二级流程已由 Computer Use / 模拟器真实盘点。  
> 使用规则：真实验证列区分“通过、部分验证、未执行”；源码存在、页面可见和端到端成功不能互相替代。

## 1. 全局导航

| 区域 | v1.7.0 源码确认 | Android 平台依赖 | iOS/macOS 迁移方向 | 真实验证 |
| --- | --- | --- | --- | --- |
| 首页 | 状态卡、日历、DDL、数据变动、下拉刷新 | Android 生命周期、导航 | 共享状态/UI；iPhone 单列，macOS 可做仪表盘 | **通过**：三张状态卡、日历/DDL 信息流与底部导航真实可见；未执行下拉刷新 |
| 应用 | 自适应网格进入 7 个核心功能 | Android Navigation Compose、drawable | 共享功能目录；macOS 使用侧栏/网格 | **通过**：七个入口均真实打开；两列大卡片网格见脱敏基线 |
| 设置 | 账号、更新、清除数据、主题、壁纸、自动同步、退出 | Context、包信息、文件选择、DataStore | 共享设置模型，系统能力分平台实现 | **通过（只读）**：上下两段列表、主题菜单、更新和清除确认弹层已查看；不改变开关/壁纸/账号 |

Android 1.7.0 使用 72dp 浮动方块式底部导航，并在启用壁纸后应用 Haze 玻璃态。Apple 端保留“导航清晰、当前项明确”的行为，不要求复制 Android 形状：iPhone 可使用底部导航，iPad/macOS 优先侧栏。

## 2. 首页

| 功能 | 源码确认行为 | 风险/平台点 | 真实验证 |
| --- | --- | --- | --- |
| 新邮件 | 显示新邮件数量，点击进入校内邮箱 WebView | Cookie 注入、WKWebView/桌面网页容器 | **通过**：SSO/Cookie 可直接进入邮箱；真实网页是未适配手机的桌面多栏布局。WebView 内容不留图 |
| 校园卡余额 | 显示余额；点击弹窗提示前往完美校园 | iOS/Android 用默认浏览器打开指定小程序链接；macOS 显示同链接二维码 | **iOS 通过**：Safari 到达用户确认的落地页；macOS 二维码和 Android 浏览器待独立运行复验，均不代填或充值 |
| 校园网余额 | 显示余额；原版通过系统分享引导微信打开充值链接 | KMP 三端按用户决定改为默认浏览器直接打开；不分享、不写剪贴板 | **iOS 通过，macOS/Android 构建通过**：Safari 打开学校域名且返回 App 会话保留；其余两端待独立运行复验 |
| 日历 | 汇总作业与考试日期 | 共享日期/数据；各平台自适应布局 | **通过（展示）**：月历与日期状态真实可见；未改变日期 |
| 作业截止提醒 | 展示临近 DDL，可进入作业详情 | 共享业务/UI | **部分验证**：提醒和信息流可见；作业详情从应用页另行验证 |
| 数据变动 | 成绩、课程表、考试、作业的新增/修改/删除提示 | Repository/数据库同步 | **部分验证**：变动区域真实存在；本次没有可控同步前后样本证明四类差异算法 |
| 下拉刷新 | 触发 DataSyncRequest | iOS 手势、macOS 工具栏刷新/快捷键 | **未执行**：避免在盘点阶段额外改变同步状态 |

## 3. 应用页七个核心入口

| 入口 | 源码确认能力 | 关键平台依赖 | Apple 迁移重点 | 真实验证 |
| --- | --- | --- | --- | --- |
| 成绩 | 学年筛选、排序、成绩详情、GPA/均分、成绩自选课程计算 | Room、ViewModel | 共享计算与状态；宽窗口表格/详情 | **通过（只读）**：汇总、筛选、排序、详情及自选模式/退出真实可用；未勾选课程 |
| 课程表 | 当前/下学期、周数选择、自动跟随当前周 | Room、DataStore、时间接口 | 共享课表；iPhone 网格，macOS 可缩放/键盘切周 | **通过（展示与控制）**：七列周视图真实显示；菜单含切换学期/选择周数，周弹层有 14 项；未切换。自动跟随仍需可控时间测试 |
| 考试安排 | 考试列表与变动 | Room、教务解析 | 共享列表/筛选；日历导出后续平台实现 | **通过（展示）**：学期控制和考试卡片列表真实可见；未修改筛选 |
| 作业 | 汇总、筛选、详情、上传、附件下载 | Android Uri、文件选择、WebView、DownloadManager/文件系统 | 共享元数据；iOS Document Picker/Share，macOS Open/Save Panel | **通过到动作边界**：列表、筛选、详情、“作业内容/附件/下载/关闭”和上传/下载按钮真实可见；未上传、下载或提交 |
| 课件 | 课程资源树、下载队列和进度 | Android 公共 Downloads、OkHttp Cookie | 共享资源树；平台文件保存与冲突处理 | **通过到动作边界**：课程可展开为资源文件，下载/日历按钮可见；未下载 |
| 教室人数评估 | 教学楼、教室人数、教室安排详情 | 外部 HTTP 服务、WebView | ATS/HTTPS 风险；桌面列表—详情 | **iOS 人数接口通过**：2026-08-02 最新构建登录后选择思源楼，真实返回数据窗口和教室容量/已用人数列表；没有 ATS、网络或解析失败。旧版教室安排 WebView 仍不在本结论内 |
| 其他功能 | 校历下载；中英文成绩单下载 | Android 文件下载、存储权限 | iOS 分享/导出；macOS 保存面板 | **通过到动作边界**：校历卡和中英文成绩单卡真实可见；未下载；订阅卡确实不可见 |

源码中“作业提醒订阅”卡片当前被注释，不应作为 1.7.0 可见功能移植；只有真实 App 显示或用户明确要求时再恢复。

## 4. 设置页

| 设置 | 源码确认行为 | Apple 迁移方向 | 真实验证 |
| --- | --- | --- | --- |
| 用户信息 | 显示姓名或“未登录” | 共享展示，敏感信息不进入截图 | **通过（展示）**：登录用户卡真实可见，图中已遮挡 |
| 检查更新 | GitHub Release 检查与下载跳转 | Apple 端改为版本信息/App Store 或项目发布页 | **通过到结果弹层**：检查更新弹层可打开并关闭；未前往下载 |
| 清除本地数据 | 确认后清除本地缓存和会话 | 明确区分退出、清会话、清全部；危险操作不在盘点中执行 | **通过到确认层**：警告、确认、取消均存在；已取消 |
| GitHub 项目 | 打开仓库链接 | 系统浏览器 | **仅确认入口**：未打开外部浏览器 |
| 主题 | System/Light/Dark | 共享主题 token，服从系统外观 | **通过到菜单层**：菜单打开，当前为跟随系统；源码/菜单定义浅色、深色、跟随系统，未切换 |
| 动态配色 | Android 系统动态色 | iOS/macOS 不照搬 Material You；映射为平台语义色 | **通过（入口/状态）**：开关真实可见，未改变 |
| 自定义壁纸 | 选图、裁剪、保存本地路径、玻璃态 | Photos/Document Picker；macOS Open Panel；权限按需请求 | **仅确认入口**：未打开选择器，未改变壁纸；玻璃态未实机验证 |
| 自动同步 | 成绩、作业、课表、考试四个开关 | 共享偏好；后台能力遵守 iOS 限制 | **通过（入口/状态）**：四项开关真实可见，未改变 |
| 更新提示 | 启停应用内更新提示 | Apple 端单独定义 | **通过（入口/状态）**：开关真实可见，未改变 |
| 退出账号 | 清理账号状态与数据 | Keychain/会话清理；关闭窗口不等于退出 | **仅确认入口**：未执行，登录会话保留 |

## 5. 非页面平台能力

| 能力 | Android 1.7.0 | 迁移边界 |
| --- | --- | --- |
| 登录 | OkHttp Cookie + Java 回调/CompletableFuture + 本地验证码；**本轮真实登录成功** | 改为协程接口；Cookie/重定向共享，凭据进入 Keychain/Keystore |
| 验证码 | PyTorch Android `model.pt`；本轮自动流程能完成一次真实登录；首次启动出现 16 KB page-size 原生库警告 | iOS 优先手动可靠路径，Core ML 独立验证；一次成功不代表模型达到可发布正确率 |
| 邮箱网页 | Android WebView + Cookie SSO **真实可用**，但加载桌面式网页，移动端布局很差 | iOS WKWebView；macOS 网页容器或外部浏览器，域名严格限定；需要原生容器导航和内容适配策略 |
| 数据库 | Room 2.6.1 | 先验证 Room KMP；迁移测试通过后再决定是否使用 SQLDelight |
| 普通设置 | Android DataStore | 跨平台设置抽象 |
| 敏感凭据 | 普通 DataStore 字符串 | 禁止照搬；Android Keystore/Apple Keychain |
| 文件 | Uri、DownloadManager、公共 Downloads；本轮只验证按钮与详情，不写入文件 | 公共 API使用 byte source/sink；平台实现选择/保存/分享 |
| 小组件 | Glance 周视图 App Widget；Android 本轮未添加到桌面，只有源码/清单证据 | 按用户决定不进入当前公版：iOS 验证性 WidgetKit/App Group 实现已删除，macOS 未实现；三端公版主应用完成后再单独立项 |

## 6. 本轮 Computer Use 盘点边界

本轮按以下顺序完成：

1. 首页正常、加载、刷新、状态卡和 DDL。
2. 应用页 7 个入口及安全可查看的二级页。
3. 设置页所有条目、弹窗和切换前状态。
4. 返回、底部导航、页面切换和滚动行为。
5. 1.7.0 专项中，成绩自选、周数控制、作业附件入口、课件展开和教室两级列表已实机观察；小组件、同名作业同步、壁纸/玻璃态和教室安排弹层保留明确未验证记录。

提交作业、充值、发送、订阅、清数据、退出账号等会改变外部或重要本地状态的动作不在默认盘点权限内。

脱敏视觉文件及其限制见 `docs/migration/visual-baseline/README.md`。WebView 和 Dialog 背景不能仅依赖 Android 可访问性文字框自动遮挡。
