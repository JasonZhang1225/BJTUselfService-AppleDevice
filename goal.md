# BJTUselfService KMP 迁移目标

## 一句话目标

保持现有 Android 应用代码完全不变，在仓库内并行建设独立的 Kotlin Multiplatform + Compose Multiplatform 应用，使新应用支持 Android、iOS 和 macOS，并让 Apple 端复用业务逻辑与主要界面，同时保留各平台应有的交互、文件、网页和安全能力。

## 当前状态

### 已完成（主线已通）
- [X] M0 基线与工具链、M1 最小 KMP 骨架、M2 共享领域层、M3 网络/登录/解析。
- [X] M4 持久化与账户安全（仅剩合法签名下 iOS Keychain 往返）。
- [X] M5 首页与八个业务切片（成绩、课程表、考试、作业、课件、校历成绩单、教室、设置）。
- [X] M6 平台能力（文件下载上传、网页容器、校内邮箱、macOS 菜单与窗口生命周期）。
- [X] M7 三端真实登录验收（核心只读流程、系统保存面板、邮箱、缓存往返已通过）。

### 等外部条件
- [ ] 真实数据变化样本（等学校侧自然产生，不造假）。
- [ ] 真实作业上传（等学校布置新作业）。
- [ ] 退出账号往返（留到有效会话不再需要时）。

### 缺权限/签名
- [ ] iOS Keychain 合法签名往返（未签名 Simulator 返回 `errSecMissingEntitlement`）。
- [ ] Apple Developer Team、真机、Archive、正式签名、公证、App Store 隐私问卷。

### 当前最优先（按顺序执行）
- [X] **M12 课程表体验整合**（日期跳转、缓存跟随修复、列表/概览交互、课程性质色块、独立全部页与交替课程半格、Android `.ics`、iOS/macOS EventKit、考试单条加入及四端门禁均已完成）。
- [ ] **M13 物理在线（phyvlab）接入**（需内网调研先行，规划中）。

> M5.5 登录页 UI + 验证码、M5.6 全部 UI 手动确认与真实数据验证已于 2026-08-09 由用户确认完成。当前进度细节见 `memory.md`（实时工作记忆）与 `history_full.md`（按里程碑归档的完整历史）。本文件只描述规划与验收标准。M12–M13 详细目标文档见 `docs/migration/`。

## 版本与功能基线

### KMP 自身版本基线（2026-08-09 起）

第一阶段（M0–M11 与 M5.5/M5.6）已收口，KMP 三端已发布 **pre-release `1.7.1-KMP`**（git tag `v1.7.1-KMP`，指向 `8498f32`）。自该版本起：

- **`v1.7.1-KMP`（git tag，`8498f32`）** 取代 `v1.7.0@419313d` 成为新功能与回归验收的对照基线；`v1.7.0` 保留为冻结 Android 原工程的对照参考。
- 后续新功能走独立里程碑（M12 起），每个里程碑规划文档放在 `docs/migration/`。
- KMP 版本号与发布节奏独立于上游 Android；未经用户明确授权，不创建版本号、提交、标签、GitHub Release 或上传构建产物。

### 上游 Android 基线（历史）

从 2026-07-29 起，**发布与功能对齐基线**曾统一调整为正式 Release `v1.7.0`，对应提交 `419313d`。本地迁移分支仍创建于 `ZJG@9d8da18`，但它只是**分支创建点**，不再代表最新功能基线。

`v1.7.0@419313d` 是 `9d8da18` 的后代，领先 9 个提交且没有分叉；它在分支创建点之上额外纳入了作业附件下载、课程表周视图小组件（已决定延后）、底部导航/壁纸/玻璃态优化、作业同步身份修复、教室人数评估教室安排修复等。详细清单与当时的验证证据见 `history_full.md` M0 节。

后续工作的约束：

- Android 行为盘点、KMP 回归测试和 Apple 端功能对齐以 `v1.7.0` 发布 APK 及 `419313d` 对应源码为准；新功能对照基线自 2026-08-09 起为 `v1.7.1-KMP`。
- 不得继续把 `ZJG@9d8da18` 或 `v1.6.1` 描述成最新功能基线；它们只用于记录历史创建点和旧版本差异。
- 当前根 Android 工程仍保持冻结。未经用户批准，不为追平 `v1.7.0` 而 merge、rebase、cherry-pick、切换分支或覆盖当前未提交改动。
- 若本地需要查看 `v1.7.0` 源码，优先放在隔离的只读参考目录或独立工作树中；不得混入 `multiplatform/` 实现和冻结的根 Android 工程。
- KMP 迁移与后续 Android Release 仍是独立工作流；未经用户明确授权，不创建版本号、提交、标签、GitHub Release 或上传构建产物。
- 2026-08-01 起，iOS/macOS 小组件不属于当前公版范围；现有 WidgetKit/App Group 验证性实现已移除，公版主应用完成前不再开发或验收小组件。

## 目标平台

| 平台    | 最低目标                                                          | 交付物                         |
| ------- | ----------------------------------------------------------------- | ------------------------------ |
| Android | 现有 `:app` 冻结；新 KMP Android target 是否替代它由后续单独决定 | 现有 APK/AAB + 新 KMP 调试构建 |
| iOS     | 优先支持 arm64 真机和 Apple Silicon 模拟器                        | Xcode App/Archive              |
| macOS   | 优先支持 Apple Silicon；是否补充 x64 在发布阶段决定               | `.app`，需要时生成 DMG       |

iOS 和 macOS 共享代码，但最终仍生成各自的平台应用，不把它们误认为同一个二进制文件。

## 核心原则

1. **Android 冻结**：现有 Android `app/`、根 Android Gradle 配置、资源和测试不修改；它只作为行为与结果参考。KMP 使用独立目录和独立 Gradle 工程。
2. **行为优先**：先复现登录、Cookie、解析、缓存和同步行为，再调整内部结构；不擅自大改后端或学校接口交互方式。
3. **纵向切片**：按用户可验证的功能迁移，而不是一次搬完所有“网络层”或“UI 层”。
4. **共享该共享的，原生该原生的**：业务、模型、状态和主要 Compose UI 尽量共享；WebView、文件、凭据、模型推理、窗口和系统集成使用平台实现。
5. **安全不能原样照搬**：不把信任所有证书、明文凭据存储、无边界明文 HTTP 当成兼容性要求。
6. **本地优先**：继续保持成绩、课表、作业等数据在本地请求与解析；除现有明确的第三方功能外，不新增上传账号、密码或验证码的自有后端。
7. **每步可回退**：每个里程碑都应有明确验证；失败时可以保留上一阶段可运行版本。

## 建议的目标结构

现有 Android 工程保持原样。新的 KMP 工程放在独立目录中，拥有自己的 `settings.gradle.kts`、构建配置和模块；预期结构如下：

```text
BJTUselfService/
├── app/                         # 现有 Android 应用：冻结，只读参考
├── build.gradle.kts             # 现有 Android 根配置：冻结
├── settings.gradle.kts          # 现有 Android 根配置：冻结
└── multiplatform/               # 新的独立 KMP 工程
    ├── settings.gradle.kts
    ├── build.gradle.kts
    ├── gradle/
    ├── composeApp/              # 共享 UI 与平台入口
    │   └── src/
    │       ├── commonMain/
    │       ├── commonTest/
    │       ├── androidMain/
    │       ├── iosMain/
    │       └── desktopMain/
    ├── shared/                  # 确有必要时拆出的业务/数据模块
    └── iosApp/                  # iOS Xcode 宿主和平台配置
```

`shared` 是否独立存在，以最小可行骨架的编译结果决定。不要仅为了目录“漂亮”增加模块，也不要通过修改现有根工程来省掉 KMP 独立配置。

## 共享边界

### 应进入共享代码

- 课程、成绩、考试、作业、课件等数据模型。
- 成绩计算、筛选、排序、状态对比和同步规则。
- Repository 接口和用例层。
- Kotlin 协程、Flow/StateFlow 状态管理。
- JSON 序列化和可跨平台的 HTML 解析。
- 网络请求协议、请求参数、响应映射和 Cookie 会话规则。
- 数据库实体、DAO 接口和迁移规则（框架支持时）。
- 主题 token、主要 Compose 组件和响应式布局规则。
- 不依赖 Android/JVM 的单元测试。

### 必须按平台实现或适配

- Android WebView / iOS WKWebView / macOS 网页容器或外部浏览器。
- Android DownloadManager、iOS 文档/分享面板、macOS 保存面板。
- Android DataStore/Keystore、Apple Keychain 和平台文件目录。
- PyTorch Android 与 Apple Core ML 验证码推理。
- Android Intent、iOS URL Scheme、macOS 菜单、窗口和快捷键。
- 通知、权限、生命周期、更新与安装逻辑。
- 平台系统栏、侧栏、返回行为、鼠标悬停和键盘操作。

## 技术方向

- UI：Compose Multiplatform + Material 3，使用窗口尺寸和输入方式做自适应，而不是复制手机布局。
- 异步：Kotlin Coroutines + Flow，逐步淘汰 Java `CompletableFuture` 回调链。
- 网络：优先评估 Ktor Client；Android 和 Apple 平台分别使用合适引擎。
- 序列化：收敛到 `kotlinx.serialization`；不继续扩大 Gson、Moshi、`org.json` 混用。
- HTML：选择确实支持所需 KMP target 的解析方案，并用现有页面样本做回归测试。
- 数据库：已完成 Room Multiplatform 与 SQLDelight 的目标适配比较，最终采用 SQLDelight 2.3.2；共享 `.sq/.sqm` 定义 schema、类型化查询和迁移，三平台分别接 Android、Native 和 JDBC SQLite driver。
- 设置：可共享普通偏好；密码、令牌和敏感 Cookie 必须进入 Keystore/Keychain。
- 依赖注入：优先构造函数注入或轻量跨平台方案；不把 Android Hilt 带入 `commonMain`。
- 验证码：保留 Android PyTorch 实现；Apple 平台转换并验证 Core ML 模型，以 `expect/actual` 或接口隔离。

具体版本必须在创建骨架时依据本机 IDE 和官方兼容矩阵选择，不能在文档阶段随意锁死。

## Apple 端体验目标

共享 UI 不等于三个平台显示完全相同：

- iPhone：触控优先，紧凑导航，常用操作在单手可达范围内。
- iPad：宽屏时使用列表—详情或侧栏，不单纯拉伸手机页面。
- macOS：侧栏、工具栏、可调整窗口、鼠标悬停、右键菜单和键盘快捷键优先。
- 所有平台：支持浅色/深色与基础动态字体；既有键盘导航和语义标签保留。用户于 2026-08-01 明确将进一步 VoiceOver/无障碍验收暂时移出本次迁移范围，不作为完成门禁。
- 动效：输入后立即反馈；可触摸/拖动的动画必须可打断并从当前视觉状态继续。
- 默认动效克制，非动量交互不使用无意义弹跳；系统开启“减少动态效果”时改用淡入淡出或静态反馈。
- 进入和退出遵循同一路径，弹层从触发源建立空间关系，不用动画掩盖网络等待。

## 迁移里程碑

> 说明：每个里程碑只保留**范围与验收标准**；已完成状态的细节与验证证据已归档到 `history_full.md` 对应节。

### M0：基线与工具链

状态：**主体完成**。

- 以只读方式记录当前 Android 构建、测试和关键页面基线；不为基线工作修改现有 Android 文件。
- 获取并核对 `v1.7.0@419313d` 的隔离源码参考与正式发布 APK，不把 `ZJG@9d8da18` 工作树误当成完整 1.7.0 源码。
- 为 1.7.0 的当前周修复、成绩自选、作业附件、作业同步、小组件、导航/视觉和教室安排修复建立明确回归基线。
- 使用命令行验证 JDK、Gradle、Android SDK、Xcode 和模拟器。
- 命令行用于证明构建与测试；Computer Use 和视觉检查用于证明真实界面与真实行为，二者不能互相替代。
- 必须在本机运行并登录原 Android App，遍历所有可到达的一级入口和核心二级页面，理解每个区域显示什么、数据从哪里来、能执行什么操作、有哪些加载/空/错误/成功状态以及依赖哪些 Android 平台能力。
- 为原 Android App 建立逐页功能清单和脱敏视觉基线，作为 iOS/macOS 实现的行为对照；不能仅根据源码猜测页面用途。
- 不登录开发者账号、不创建证书、不接受新协议，除非用户当场授权或接管。

验收：当前冻结 Android 工程的 debug 构建和单元测试结果已记录；`v1.7.0` 正式发布 APK 已完成登录后的逐页功能与视觉盘点；1.7.0 隔离源码参考可追溯到 `419313d`；工具链问题被明确列出。

### M1：最小 KMP 骨架

状态：**完成**。

- 在 `multiplatform/` 中建立独立 KMP/Compose Multiplatform 工程，不修改、删除或替换现有 `:app` 和根 Android Gradle 文件。
- 显示一个共享页面，并分别运行 Android、iOS Simulator、macOS。
- 建立 source set、资源和测试目录。

验收：已通过。三个目标各自显示带平台名的共享页面；冻结根 Android `:app:testDebugUnitTest :app:assembleDebug` 复跑成功，受保护路径聚合 SHA-256 前后一致。

### M2：共享领域层

状态：**完成**。

- 优先迁移纯 Kotlin 数据类、成绩计算、筛选、排序和日期逻辑。
- 去除共享代码中的 Android、JVM 和全局 `Context` 依赖。
- 把现有关键单元测试迁入 `commonTest`，补充跨平台边界测试。

验收：共享测试覆盖核心计算，Android 的对应结果不变。

### M3：网络、登录与解析

状态：**完成**。

- 将 Java 回调/`CompletableFuture` 登录流程改造成协程接口。
- 建立跨平台 HTTP、Cookie 和重定向处理。
- 迁移 MIS、教务和智慧课程平台解析，并保存脱敏响应样本做测试。
- 修复信任所有证书的实现；盘点明文 HTTP 和 ATS 影响。

验收：在不把账号信息写入日志的条件下，Android 与至少一个 Apple 目标能完成测试账号登录和基础数据获取。

### M4：持久化与账户安全

状态：**主体实现完成，签名运行验收待补**。M4 的三项功能验收已满足，但未签名 iOS Simulator 因缺少合法应用签名 entitlement 返回 `errSecMissingEntitlement (-34018)`，仍须在获得 Apple Development 签名身份后补 Keychain 运行往返，因此不把整个里程碑标记为完整完成。

- 迁移 Room/SQLite 数据和普通设置。
- 敏感凭据改为 Android Keystore/Apple Keychain。
- 定义升级、清除账号、数据库迁移和失败恢复行为。

验收：重启后普通缓存可恢复；退出登录能清理敏感状态；数据库升级测试通过。

### M5：核心功能纵向迁移

状态：**已完成**。首页和八个业务切片（成绩、课程表、考试、作业、课件、其他功能、教室、设置）已完成首版实现与验收。剩余真实数据变化样本与真实作业上传属「等外部条件」，不阻塞里程碑完成。已完成细节见 `history_full.md` M5 各子节。

按以下顺序逐项形成可运行切片：

1. 登录与主页状态、周议程、DDL 和数据变动信息流（真实 iOS 刷新与缓存落地已通过；仍缺服务器真实变化样本）。
2. 成绩。
3. 课程表。
4. 考试安排。
5. 作业列表与详情。
6. 课件与课程资源。
7. 其他功能、教室和设置（均已完成首版实现）。

每个切片都包含：Repository、状态、共享 UI、Android 回归、iOS 验证、macOS 自适应验证。

课件切片的 v1.7.0 树形资源、递归下载、教学日历、明文 IP 风险和 Apple 信息架构审计见 `docs/migration/m5-courseware-plan.md`。

### M5.5：登录页 UI 优化 + 验证码自动识别

状态：**已完成**（2026-08-09 用户确认）。登录页视觉/交互对齐 Apple 设计验收；验证码提供「显示 → 刷新 → 自动识别 → 失败回退手动」完整链路，Android 用 PyTorch、Apple 用 Core ML，自动识别失败最多 3 次后回退手动。详细实现与验证证据见 `history_full.md` 相关节。

**目标**：让登录页在视觉与交互上达到 Apple 设计验收标准，并为验证码提供“显示 → 刷新 → 自动识别 → 失败回退手动”的完整可靠链路。

**范围（分两步，UI 与识别解耦，互不阻塞）**：

- **5.5.1 登录页 UI 优化**（先做）：
  - 视觉/交互对齐 Apple 设计验收要求（响应、空间一致、克制弹性、材料层级、排版可伸缩、辅助功能）。
  - 表单、按钮、加载/错误/成功状态的明确反馈。
  - 当前：登录卡片只保留账号、密码、安全保存和全宽登录按钮；验证码图片、刷新与答案框只在三次自动登录失败后的恢复弹框出现。iOS 原生账号/密码框使用 14pt 圆角、动态浅深色、Return 链路和全页键盘收起/避让，互操作承载层使用当前 `surface` 色，暗色不再出现整块黑底。后续继续收敛跨页 token，不以当前登录页阻塞真实交互。
- **5.5.2 验证码自动识别服务**（独立增强，不阻塞登录）：
  - 沿用下方“验证码策略”的 7 层对齐方案。
  - Android 保留 PyTorch；Apple 端 Core ML 转换 + `expect/actual` 或接口隔离。
  - 自动识别失败无缝回退手动填写；每次使用新验证码且最多尝试 3 次，不无限重试。
  - 当前：24 张固定真实冒烟集 Apple/Android 表达式 21/24、argmax 24/24 一致；自动识别默认启用，模型不可用、识别失败与瞬时网络异常均受 3 次上限约束，公版前仍需至少 300 张独立留出集。

**平台差异**：UI 在 Android/iOS/macOS 共享 Compose 层，但需按平台窗口宽度和输入方式自适应；验证码推理在 Android 用 PyTorch、Apple 用 Core ML。

**验收**：登录页通过三端真实视觉/交互验收；验证码在固定脱敏测试集上达到用户约定的正确率后才默认开启自动识别，否则保持手动。

### M5.6：用户手动确认全部 UI（M5.5 后）

状态：**已完成**（2026-08-09 用户确认）。触发条件：M5.5（登录页 UI + 验证码模型）整个阶段完成后进入。

**范围**：用户手动确认**全部**页面的 UI 细节，不限于登录页。覆盖已完成的业务页面（首页、成绩、课程表、考试、作业、课件、校历成绩单、教室、设置）和登录页，按 M0 同一份功能清单逐页对照。

**验收**：每页明确标记"已对齐、合理的平台差异、需调整"，由用户确认通过后才进入真实数据验证；不允许以"编译通过/模拟器通过"代替用户手动确认。

### M6：平台能力

状态：**已完成**。作业文件下载、multipart 上传协议、三端文件网关、网页容器与校内邮箱、macOS 原生菜单与窗口生命周期均已完成并有三端构建证据；真实作业上传属「等外部条件」。已完成细节见 `history_full.md` M6 节。

- WKWebView/桌面网页能力和 Cookie 同步。
- 文件选择、上传、下载、预览和分享。
- 日历、外部链接、通知、菜单与快捷键。
- 验证码图片展示与手动输入作为首版可靠路径。
- Core ML 自动识别作为可选增强，按输入张量、原始输出和解码结果三层做一致性与精度回归；转换失败不阻塞其他功能。

课程表小组件不再属于当前 M6：2026-08-01 已按用户决定移除 iOS WidgetKit 扩展、App Group entitlement、共享快照发布和预览入口。等 Android/iOS/macOS 公版主应用完全稳定后，再依据 1.7.0 周视图基线另立切片设计、实现和验收。

验收：文件与网页流程在各自平台符合系统习惯；手动验证码流程可用。只有固定测试集达到预先约定的正确率，才启用自动识别。

## 验证码策略

Apple 端曾出现模型可以生成或加载、但实际验证码识别很差的情况，因此禁止再次“直接转换后接入”。首版采用以下分层方案：

1. **可靠基线**：显示服务器返回的验证码图片，允许刷新和手动填写计算结果；自动识别失败时无缝退回手动填写，不反复重试导致账号或接口受限。
2. **样本集**：通过合规的人工测试收集固定、脱敏且仅用于本地验证的验证码图片与正确表达式/答案；不提交真实账号、Cookie 或关联会话标识。
3. **预处理对齐**：复现 Android 当前的 `130×42` 缩放、RGB、CHW、`[0,1]` 归一化，明确插值、图片方向、颜色空间和透明背景处理。
4. **张量对齐**：同一图片分别导出 Android 与 Apple 输入张量，对比形状、通道和数值误差。
5. **输出对齐**：比较 PyTorch 与 Core ML 的原始 logits/类别序列，确认输出维度顺序，不只比较最终字符串。
6. **解码对齐**：核对 15 类字符表、8 个时间步、重复字符折叠、blank 类和算式合法性。
7. **启用门槛**：测试集正确率和失败回退行为达到用户确认的标准后，才默认开启自动识别。

可能的误差源包括缩放插值、EXIF/图片方向、RGBA 合成、RGB/BGR、CHW/NHWC、Core ML 输出轴顺序、算子转换数值误差和 CTC 解码差异。必须用测量排除，不能靠猜测反复调参数。

使用 Computer Use 查看真实流程时，可以按用户于 2026-07-29 的明确授权，在实际登录动作发生时读取仓库根目录 `MisSecret.md`，并把其中的账号和密码直接填入本机原 Android App，或本机运行的 iOS/macOS 移植版所复现的既有北交大登录流程。不得把凭据输出到对话、终端、日志、截图、测试、剪贴板记录或第三方服务，也不得用于其他目的。用户于 2026-07-30 进一步表达持续授权：后续获取、识别、填写和提交同一北交大登录流程的 CAPTCHA 不希望逐次确认；若执行环境存在不可豁免的 CAPTCHA 动作时确认规则，则仅按该规则询问最小必要的一次。账号来源、登录目标或凭据接收方发生变化时仍须停止并重新确认。自动识别暂时无法达标，不影响登录页面、网络会话和其他功能继续迁移。

### M7：Apple 设计与可访问性

状态：**已完成**。macOS 已完成首页和九入口首轮真实会话验收；iOS 已恢复有效会话并完成核心只读流程与系统保存面板。已完成细节见 `history_full.md` M7 节。

- iPhone、iPad、窄/宽 macOS 窗口逐一检查。
- 分别登录 iOS 与 macOS App，按 M0 的同一份功能清单逐页复测，并与原 Android App 的行为和视觉基线对照。
- 验证导航可预测、控件映射清晰、加载/完成/警告/错误反馈齐全。
- 既有动态字体、减少动态效果、减少透明度和高对比度实现保留；用户已将进一步无障碍专项验收移出当前范围。
- 检查拖动、弹层和页面切换的可打断性与空间一致性。

验收：iOS 与 macOS 均完成登录后的逐页截图和交互检查；原 Android 功能清单中的每项均标明“已对齐、经批准的平台差异、尚未完成或不可行”，不存在无记录遗漏，也不存在必须横向滚动、文字截断或无法通过键盘访问的核心操作。

### M8：发布准备

状态：**进行中**。Apple 两端可独立闭环的发布审计（图标、隐私清单、加密声明、macOS 打包）已完成；正式版本号、Apple Developer Team、真机、Archive、Developer ID/App Store 签名、公证和隐私问卷仍待发布阶段完成。已完成细节见 `history_full.md` M8 节。

### M9：成绩课程性质映射

状态：**已完成**。成绩接口无课程性质列，性质从培养方案 `/training/training/program/` 与全部 `stuview/<id>/` 交叉比对产出课程号→必修/限选/任选/体育映射；自选模式五类三态筛选；SQLDelight schema v2→v3。细节见 `history_full.md`「成绩课程性质映射」节与 `docs/migration/m9-grade-course-type-plan.md`。

### M10：平台原生导航与动效

状态：**已完成**。紧凑端二/三级页导航交给平台原生容器（Android 系统 Activity、iOS UIKit `UINavigationController`），共享 `NavDisplay` 退居宽屏与回退路径；底栏一级 tab 永不触发原生 push。细节见 `history_full.md` M10 节与 `docs/migration/native-platform-navigation-plan.md`。

### M11：教室占用查询

状态：**已完成**。新功能，原冻结 Android 1.7.0 没有。教务 `room_view` 两级楼→占用，`jxlh` 数字 ID，学期/周弹层，bksy 校历 `executePublic` 公开通道，弹层选周改详情 hostScope 修「同步中」卡死。细节见 `history_full.md` M11 节。

### M12：课程表体验整合

状态：**已完成**。M12a 日期跳转与缓存跟随修复、跨本学期/选课学期自动切换，M12b 带图标的列表/概览表格、课程号性质配色、独立最左“全部教学周”页与交替课程半格，M12c 课程重复系列与考试单条加入均已闭环；紧凑 UI 文案、地点倒序 `-` 展示/导出和宽屏 AppKit 原生触摸板分页均完成。选课课表改用下一学期校历，真实核对 `2026-2027-1` 第 1 周为 `2026-09-07`；macOS EventKit 首次新增 21 个系列、二次更新 21。Desktop/Android/iOS/macOS 构建门禁均通过；用户已确认实体触摸板与跨学期“前往日期”可用。范围与证据见 `docs/migration/m12-course-schedule-plan.md`。

当前 Apple 构建环境：系统 `xcode-select` 已于 2026-08-11 经授权切换到 Xcode Beta 27.0（`/Applications/Xcode-beta.app/Contents/Developer`，Build `27A5228h`）。

### M13：物理在线（phyvlab）接入

状态：**规划中（调研先行）**。接入 `phyvlab.bjtu.edu.cn`，复用 MIS SSO 握手模式；已实测外网 443 不通，实现与验收必须等内网/VPN。范围、调研清单、认证复用路径见 `docs/migration/m13-phyvlab-integration-plan.md`。

### M14：Windows 桌面端移植

状态：**已完成（已并入 main 并随 1.7.3-KMP 发布）**。KMP Windows 桌面应用，UI 复用 commonMain 共享 Compose，功能对齐 `1.7.2-KMP-A` 之后 1 个提交（`93b3d0a`），验证码自动识别复用原版 Android torch 模型（`BJTUCaptcha.pt`）。

- 模块：`multiplatform/windowsApp`（`jvm("windows")` target + `implementation(project(":shared"))`），不修改冻结 Android 工程与 macOS desktopApp。
- 平台实现：DPAPI 凭据保险库、`%LOCALAPPDATA%` 缓存、AWT 文件网关、系统浏览器网页、Ktor CIO。
- 验证码：DJL PyTorch 引擎加载同一 `.pt`；已与 Python torch 参考 logits 逐值对齐（max abs diff 0.000012，argmax 一致）。
- 打包：jpackage EXE/MSI（需完整 JDK）；系统级安装到 `Program Files`，推荐 MSI 以便启动时前台弹出 UAC；卸载清除用户缓存与记住的登录。
- 规划与验收：`docs/migration/windows-port-plan.md`。真实登录会话复测与 24 张冒烟集正确率评测待样本/账号条件。

- Android、iOS、macOS 分别进行 release 构建验证。
- 核对 Bundle ID、版本号、图标、权限说明、隐私清单、网络安全和签名。
- 准备可编辑的发布说明；用户确认后才提交、打标签、推送或发布。

可编辑草稿已写入 `docs/migration/m8-release-notes-draft.md`；正式版本号、日期、支持/隐私网址和已知限制仍须发布者审阅，当前没有执行任何提交、标签、推送或发布操作。

验收：三端 release 构建产物可安装运行，已知限制有明确记录。

## 完成定义

只有同时满足以下条件，整个迁移目标才算完成：

- 现有 Android 工程文件与创建 `ZJG` 分支时相比没有被 KMP 工作修改。
- 新 KMP 应用的功能范围和行为对照以 `v1.7.0@419313d` 为准，而不是停留在 `ZJG@9d8da18`。
- 新 KMP Android target 如被保留，其行为另行验证，不以替换旧 Android 为默认目标。
- iOS 真机或正式可签名构建完成核心业务流程。
- macOS Apple Silicon 应用完成核心业务流程并符合桌面交互。
- 共享层不存在 Android `Context`、Android ViewModel、JVM-only Java API 等平台泄漏。
- 敏感凭据使用系统安全存储，日志和测试样本不含真实账号、密码、Cookie。
- 不再通过全局信任所有 TLS 证书来维持登录。
- 明文 HTTP 的保留范围、原因和风险已被明确处理。
- 三端测试、构建和关键交互验证有可复现记录。
- 文档与实际模块、命令和已知限制一致。

## 非目标

- 不在迁移初期重做所有视觉风格。
- 不为了跨平台而引入新的账号代理后端。
- 不保证第一阶段就上架 App Store 或完成开发者签名。
- 不默认支持 Intel Mac；只有明确需求和验证后再加入。
- 不在没有张量、原始输出和固定测试集证据时声称验证码模型已经成功转换。
- 不擅自提交、打标签、推送、创建 PR 或发布安装包。

## 决策记录

历史决策与新增决策统一归档在 `history_full.md`「决策记录」节，按日期追加。
